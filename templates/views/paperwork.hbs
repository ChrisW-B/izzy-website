{{>header}}
<div class="container">
  <h1>Paperwork</h1>
</div>
<div class="container">
  <div class="row">
    <div class="col-sm-8 col-md-9">
      <h4 class="text-weight-normal">Showing {{ data.posts.results.length}} post.</h4>
      <div class="blog">
        {{# each data.posts.results}}
          <div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="post">
            <h2><a href="{{postUrl slug 'paperwork'}}">{{{title}}}</a></h2>
            <p class="lead text-muted">{{{tagList tags prefix="Posted in "}}}
              {{#if author.name.first}}by {{author.name.first}}{{/if}}
            </p>
            {{#if images}}<img src="{{{cloudinaryUrl images.[0] width=160 height=160 crop='fit' }}}" class="img pull-right">{{/if}}
            <p>{{{content.brief}}}</p>
            {{#if content.extended}}
              <p class="read-more"><a href="{{postUrl slug 'paperwork'}}">Read more...</a></p>
            {{/if}}
          </div>
          {{/each}}
      </div>
    </div>
    <div class="col-sm-4 col-md-3">
      <h2>Categories</h2>
      <div style="margin-top: 70px;" class="list-group">
        <a href="/paperwork" class="active list-group-item">All Tags</a>
        {{# each data.tags }}
          <a href="{{categoryUrl 'paperwork' key }}" class="false list-group-item">{{name}}</a>
        {{/each}}
      </div>
    </div>
  </div>
  {{>pagination}}
</div>
